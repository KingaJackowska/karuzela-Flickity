$('.main-carousel').flickity({
    // options
    cellAlign: 'left',
    contain: true,

    // Disable dragging
    freeScroll: true,
    
    // disable previous & next buttons and dots
    prevNextButtons: false,
    pageDots: false
  });